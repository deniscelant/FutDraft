let positions = {
    positionGK: Card,
    positionZG: Card,
  };
  
class LineUp{
  constructor(positions){
    this.positions = positions
  }
}

positions.map
if(position == Iposition){
  thisPosition = positions.map()
}

//IPosition
//LineUp.push(Card)
//
//
//
//
onclick divs =>
positions.forEach(pos => {
  if(pos == Iposition){
  positions.pos == 
} 
});

LineUp.ZG = new Card(basics, position, stats)