class Card {
    constructor(basics,position,stats) {
        basics = this.basics;
        position = this.position;
        stats = this.stats;
    }
    
    renderCard() {}
  }
  

  new Card(basics, position, stats)