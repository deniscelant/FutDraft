class Card {
    constructor(playerBasics,position,stats) {
        playerBasics = this.playerBasics;
        position = this.position;
        stats = this.stats;
    }
    
    renderCard() {}
  }
  